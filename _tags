true: bin_annot, debug, safe_string

<lib>: include
<tests>: include

<**/*.*>: package(lwt), \
          package(lwt.ppx), \
          package(uri), \
          package(cohttp.lwt), \
          package(ppx_deriving.std), \
          package(containers), \
          package(nocrypto), \
          package(conduit)
